# Local configuration files with environment-specific settings
pi5/config.py

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
pi5/.env
pi5/data/*
pi5/data/race_setup.json
pi5/data/animation_assignments.json
!pi5/data/animation_registry.json
pi5/animation_params.json

# Font sources: Impact is licensed with Windows; only the traced ddm_font.h is committed
*.ttf
*.TTF

# La Quiniela betting-model event log now lives on pi5 (pi5/data/quiniela_YYYY-MM-DD.jsonl,
# covered by pi5/data/* above); splash_display/logs/ held the old splash-side log and stays
# ignored so files left on a Pi from before the move keep out of git status
splash_display/logs/
